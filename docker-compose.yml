version: '3'
volumes:
  dados: 
services:
  db: 
    image: postgres:9.6
    volumes:
      # Volume dos dados
      - dados:/var/lib/postgresql/data
      # Volume para apontar para os scripts 
      - ./scripts:/scripts

